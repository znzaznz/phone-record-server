services:
  audio-stt-producer:
    build: .
    ports:
      - "10083:8000"
    env_file:
      - .env
    volumes:
      - /volume1/openclawdata/need-fresh-data/phone-record-server:/app/shared/output
